# Auth Service

This is the authentication microservice for the CleanNation platform. It handles user registration, login, JWT token signing, and token refreshing.

## Step 1: Initialization
In this step, I have created the directory structure and initialized the service using Bun.

<br>

## Step 5: Service Implementation

In this step, I have implemented the core authentication logic, controllers, routes, and the main server entry point to wire everything together.<br>
The service now supports registration, login, refresh tokens, logout, and self-discovery, all while adhering to the security best practices.<br>
The main application is now fully wired and functional.<br>
<br>
Updated: Database connection setup in `src/db/prisma.ts`.<br>
